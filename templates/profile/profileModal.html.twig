{% extends 'base.html.twig' %}

{% block title %}My Modal{% endblock %}

{% block body %}

<div class="">
  {{ form_start(userForm) }}
    {{ form_widget(userForm) }}
  {{ form_end(userForm) }}
</div>
<div class="">
  {{ form_start(avatarForm) }}
    {{ form_widget(avatarForm) }}
  {{ form_end(avatarForm) }}
</div>
     
{% endblock %}