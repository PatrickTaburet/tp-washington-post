{% extends 'base.html.twig' %}

{% block title %}Register{% endblock %}

{% block body %}
    <h1 id="toto">Register</h1>
<div>
    {{ form_start(registrationForm, {'attr': {'id': 'registrationForm'}}) }}
        {{ form_row(registrationForm.email) }}
        {{ form_row(registrationForm.plainPassword, {
            label: 'Password'
        }) }}
        {{ form_row(registrationForm.name) }}
        {{ form_row(registrationForm.firstname) }}

        {{ form_row(registrationForm.description) }}
        {{ form_row(registrationForm.agreeTerms) }}
   <button id="submitBtn" type="submit" class="btn">Register</button>
    {{ form_end(registrationForm) }}
</div>   
{# <div class="">
    {{ form_start(avatarForm, {'attr': {'id': 'avatarForm'}}) }}
        {{ form_widget(avatarForm) }}
    {{ form_end(avatarForm) }}
</div> #}
    
     
{% endblock %}
